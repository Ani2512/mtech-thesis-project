numpy>=1.26
scipy>=1.11            # Hungarian matching in metrics
soundfile>=0.12
pyyaml>=6.0
pytest>=8.0

# Real backends — GPU runtime only
# torch>=2.3
# transformers>=4.52     # Qwen2.5-Omni support
# qwen-omni-utils         # process_mm_info for Qwen2.5-Omni
# accelerate>=0.33
# librosa>=0.10
# google-genai            # Gemini backend
# peft>=0.12              # phase 2
